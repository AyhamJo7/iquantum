{
  "name": "iquantum",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "iquantum-daemon",
    "iquantum-cli"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:dist": "bun scripts/build-dist.ts",
    "dev": "cd iquantum-daemon && bun run dev",
    "test": "vitest run",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest",
    "typescript": "latest",
    "vitest": "^4.1.6",
    "zod": "^4.4.3"
  }
}
